import {
  Button,
  Card,
  Form,
  FormItem,
  Input,
  Select,
  Option,
  Alert,
} from "element-plus";

const components = [Button, Card, Form, FormItem, Input, Select, Option, Alert];

export default components;
